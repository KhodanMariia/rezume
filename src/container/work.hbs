{{!--
Це код container файлу для work сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/work"
--}}

<div>
    {{#with main}}
    <main>
        <section>
            {{>title data='Work history' big=true}}
            <div>
                <ul>
                    {{#each works}}
                    <li>
                        <div>{{position}}</div>
                        <br>
                        <div>
                            {{duration.from}}-
                            {{#if duration.to}}{{duration.to}}
                            {{else}}
                            (досі працюю)
                            {{/if}}
                        </div>
                        <br>

                        <div>
                            {{#unless company.url}}
                            <b>{{company.name}}</b>
                            {{else}}
                            {{>link text=company.name href=company.url blanc=true noLine=true }}
                            {{/unless}}
                        </div>

                        <br>
                        <br>
                        <div>
                            <strong>
                                Projects ({{projectAmount}})
                            </strong>
                        </div>
                        <br>
                        <ul>
                            {{#each projects}}
                            <li>
                                <div>{{> link text=name href=url blanc=true noLine=true }}</div>
                                <br>
                                <div>
                                    <p> {{about}}</p>
                                </div>
                                <br>
                                <div>
                                    <em>
                                        <u>
                                            Project stacks({{stackAmoGnt}})
                                        </u>
                                    </em>
                                </div>
                                <br>
                                <div>
                                    {{#each stacks}}
                                    <ul>
                                        <li>{{name}}</li>
                                    </ul>
                                    {{/each}}
                                </div>
                                <br>
                                <div>
                                    <em>
                                        <u>
                                            Project awards({{awardAmoGnt}})
                                        </u>
                                    </em>
                                </div>
                                <br>
                                <div>
                                    {{#each awards}}
                                    <ul>
                                        <li>
                                            {{name}}
                                        </li>
                                    </ul>
                                    {{/each}}
                                </div>
                            </li>
                            {{/each}}
                        </ul>
                    </li>
                    <br>
                    {{/each}}
                </ul>
            </div>
        </section>
    </main>
    {{/with}}
</div>

{{!--
Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
Адже наш layout/default.hbs має вже структуру HTML
--}}